# Path A — AI generation (server-only; never expose to client)
OPENAI_API_KEY=
# Optional overrides:
# OPENAI_CHAT_MODEL=gpt-4o-mini
# OPENAI_MODERATION_MODEL=text-moderation-latest

# Fal (Fast SDXL). Use one of:
FAL_KEY=
# FAL_API_KEY=

# Already used elsewhere — required for uploading generated images + signed URLs
# SUPABASE_SERVICE_ROLE_KEY=
# NEXT_PUBLIC_SUPABASE_URL=
